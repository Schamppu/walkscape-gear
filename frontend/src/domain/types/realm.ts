// ---------------------------------------------------------------------------
// Realms
// ---------------------------------------------------------------------------

export type Realm = {
  id: string;
  name: string;
  icon: string;
  color: string;
  reputation: string | null;
  locationCount: number;
}